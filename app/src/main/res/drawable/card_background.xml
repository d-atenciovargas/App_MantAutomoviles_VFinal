<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Color de fondo -->
    <solid android:color="#2E2E44" />

    <!-- Esquinas redondeadas -->
    <corners android:radius="8dp" />

    <!-- Borde opcional -->
    <stroke
        android:width="1dp"
        android:color="#444555" />
</shape>
